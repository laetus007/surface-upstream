{{ attach_library('surface/block') }}

{% set classes = [
  'block',
  attributes ? attributes.class
]|join(' ')|trim %}

{% set title_classes = [
  'block__title',
  title_attributes ? title_attributes.class
]|join(' ')|trim %}

{% set content_classes = [
  'block__content',
  content_attributes ? content_attributes.class
]|join(' ')|trim %}

<div class="{{ classes }}" {{ attributes ? attributes|without('class', 'id') }}>
  {{ title_prefix }}
  {% if label %}
    <h2 class="{{ title_classes }}" {{ title_attributes ? title_attributes|without('class') }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}
  <div class="{{ content_classes }}" {{ content_attributes ? content_attributes|without('class') }}>
    {% block content %}
      {{ content }}
    {% endblock %}
  </div>
</div>
