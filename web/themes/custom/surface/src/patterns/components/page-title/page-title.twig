{% set classes = [
  'page-title',
  title_attributes ? title_attributes.class
]|join(' ')|trim %}

{{ title_prefix }}
{% if title %}
  <h1 class="{{ classes }}" {{ title_attributes ? title_attributes|without('class') }}>{{ title }}</h1>
{% endif %}
{{ title_suffix }}
